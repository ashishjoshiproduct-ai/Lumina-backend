services:
  - type: web
    name: lumina-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn lumina-backend:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
